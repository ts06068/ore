name: package-checks
on:
  push:
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: |
            packages/sdk/package-lock.json
            web/package-lock.json
      - run: python -m pip install uv
      - run: uv sync --locked --extra dev --extra scholarly
      - run: uv run playwright install --with-deps chromium
      # The HTTP integration tests serve UI deep links from web/dist.
      # Build and test the SDK/console before creating the test server.
      - name: Build and verify release packages
        run: uv run python scripts/build_release.py
      - name: Run Python integration tests
        run: uv run pytest -m 'not live'
      - uses: actions/upload-artifact@v4
        with:
          name: ore-local-release
          path: dist/
