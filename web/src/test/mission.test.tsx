import {describe,it,expect,vi} from 'vitest';
import {render,screen,waitFor} from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import MissionForm from '../components/MissionForm';
describe('mission authoring',()=>{
 it('submits instructions, source scope, artifact requirements and worker budget',async()=>{const user=userEvent.setup();const create=vi.fn(async(_mission:Record<string,unknown>,_start:boolean)=>undefined);render(<MissionForm sources={[{id:'crossref',name:'Crossref'}]} models={[]} profiles={[]} onClose={()=>undefined} onCreate={create}/>);await user.type(screen.getByLabelText('Mission name'),'Evidence review');await user.type(screen.getByLabelText(/^Instructions/),'Collect original articles and all supplements.');await user.type(screen.getByLabelText(/^Search query/),'transplantation');await user.click(screen.getByRole('button',{name:'Launch mission'}));await waitFor(()=>expect(create).toHaveBeenCalledTimes(1));expect(create.mock.calls[0]?.[0]).toMatchObject({name:'Evidence review',instructions:'Collect original articles and all supplements.',sources:['crossref'],artifact_roles:['main_pdf','supplement'],query:'transplantation',limits:{max_agent_workers:2},routing:{mode:'auto'}});expect(create.mock.calls[0]?.[1]).toBe(true);});
 it('rejects inverted publication years before calling the server',async()=>{const user=userEvent.setup();const create=vi.fn(async(_mission:Record<string,unknown>,_start:boolean)=>undefined);render(<MissionForm sources={[{id:'crossref'}]} models={[]} profiles={[]} onClose={()=>undefined} onCreate={create}/>);await user.type(screen.getByLabelText(/^Instructions/),'Collect articles.');await user.clear(screen.getByLabelText('From year'));await user.type(screen.getByLabelText('From year'),'2025');await user.clear(screen.getByLabelText('Through year'));await user.type(screen.getByLabelText('Through year'),'2020');await user.click(screen.getByRole('button',{name:'Launch mission'}));expect(await screen.findByRole('alert')).toHaveTextContent('start year');expect(create).not.toHaveBeenCalled();});
});
